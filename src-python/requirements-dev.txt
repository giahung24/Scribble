# Dev/test dependencies — NOT installed for production PyInstaller builds.
# Install with: src-python/.venv/bin/pip install -r src-python/requirements-dev.txt
-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.23
